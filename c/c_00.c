# include <stio.h>

int main()      // 0을 반환해 주기 위함
{
    printf("Hello, C")

    return 0;   // 0을 반환하면 정상 작동
}

// gcc 파일명 : build 파일 생성
// ./build파일명 : 파일 실행

// rm a.out : 파일 제거
// gcc 파일명 -o 이름지정
